// public/js/views/homeView.js
/**
 * Controlador de la vista 'home'
 *
 * Responsabilidad:
 * - Inicializar comportamientos específicos de la vista de inicio cuando se
 *   inyecta en el DOM (por ejemplo, enlaces dinámicos o métricas).
 * - Recibe el contenedor DOM (la sección cargada) como argumento.
 */
let initialized = false;

export function initHomeView(container) {
  if (initialized) return;
  // Actualmente no hay lógica dinámica; conservar el hook para futuras mejoras.
  initialized = true;
}

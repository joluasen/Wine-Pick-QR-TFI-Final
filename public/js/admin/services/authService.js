/**
 * authService.js
 * 
 * ARCHIVO ELIMINADO - Ya no se requiere autenticación en la aplicación
 * 
 * Las funciones checkAuth(), logout(), redirectToLogin(), redirectToHome()
 * han sido removidas. La aplicación funciona sin login.
 */

// FUNCIONES ELIMINADAS
export async function checkAuth() { return true; }
export async function logout() {}
export function redirectToLogin() {}
export function redirectToHome() {}


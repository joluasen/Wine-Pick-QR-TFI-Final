# Wine-Pick-QR Environment Configuration - TEMPLATE PARA PRODUCCIÓN
# ====================================================================
#
# INSTRUCCIONES:
# 1. Copiar este archivo a .env en la raíz del proyecto
# 2. Reemplazar TODOS los valores marcados con [CAMBIAR]
# 3. Generar un nuevo JWT_SECRET con: php -r "echo bin2hex(random_bytes(32));"
# 4. NUNCA subir el archivo .env a repositorios públicos
#
# ====================================================================

# Entorno de ejecución
# IMPORTANTE: Mantener en 'prod' para hosting
WPQ_ENV=prod

# Base de datos
# [CAMBIAR] Usar credenciales proporcionadas por tu hosting
DB_HOST=[CAMBIAR_HOST_BD]
DB_NAME=[CAMBIAR_NOMBRE_BD]
DB_USER=[CAMBIAR_USUARIO_BD]
DB_PASS=[CAMBIAR_PASSWORD_BD]
DB_CHARSET=utf8mb4

# URL base de la aplicación
# [CAMBIAR] Usar tu dominio real con HTTPS
BASE_URL=https://[CAMBIAR_TU_DOMINIO]

# JWT Secret para autenticación
# [CAMBIAR] Generar uno nuevo con: php -r "echo bin2hex(random_bytes(32));"
# CRÍTICO: Mantener en secreto, no compartir
JWT_SECRET=[CAMBIAR_GENERAR_NUEVO_SECRET]

# Nombre de la cookie de autenticación (opcional)
AUTH_COOKIE_NAME=wpq_auth
